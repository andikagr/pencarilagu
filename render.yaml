services:
  - type: web
    name: pencarilagu
    env: node
    buildCommand: pip install yt-dlp && npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
